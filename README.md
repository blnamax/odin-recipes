I first went to GitHub and created a new repository called odin-recipes without adding a README file. Then I copied the SSH URL of the repository and opened my Ubuntu virtual machine. In the terminal, I used pwd to confirm my current location and ls to check my folders, then entered the repos directory using cd repos. Once inside, I cloned the repository using git clone with the SSH link. After cloning, I entered the project folder, created a README.md file, and opened the project in VS Code using code . to begin working on it.

Primero fui a GitHub y cree un nuevo reposito 
llamado odin-recipes sin agregar un archivo README. Luego copie el SSH URL del repositorio y lo abri en mi maquina virtual Ubuntu. En la terminal, use 'pwd' para confirmar my localidad y 'ls' para chequear mis carpetas, luego entre al repositorio repos usando 'cd repos'. Unca vez dentro. Clone el repositorio usando ;git clone' con el lin SSH, despues clonando, entre a la carperta, cree un archivo README.md usando 'touch', y abri el projecto en vscode, usando 'code .' para empezar a trabajar.